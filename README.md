# Land Record Hub

You are acting as a senior full-stack engineer. my api key is AQ.Ab8RN6LZfrMiabz2dDvakLn7KgABTOpVn1RTDiPiIDX2l2c1mg       also use lovable built in backend cloud support

Build PHASE 1 of a production-oriented hackathon MVP called:

LAND RECORD INTELLIGENCE SYSTEM

Subtitle:
AI-Powered Land Record Intelligence & Verification System

This is the foundation phase only.

IMPORTANT:
Do NOT attempt to build the entire application in this step.

Do NOT spend implementation effort on:

advanced visual polish

animations

green scanner animation

charts

sophisticated verification workbench

advanced document viewer

AI extraction UI

reconciliation UI

integrity score UI

audit timeline UI

Those will be implemented in later phases.

The objective of this phase is to create a REAL, FUNCTIONAL backend foundation that later phases can build upon.

The system must use real persistent data and real backend operations.

Do NOT create a static/mock-only frontend.

==================================================

TECHNOLOGY
==================================================

Use:

Frontend:

React

Vite

TypeScript

Tailwind CSS

shadcn/ui

Lucide icons

Backend:

Lovable Cloud

PostgreSQL

Authentication

Storage

Edge Functions/server-side functions

Row Level Security

Do NOT use:

Firebase

Python/FastAPI

separate backend servers

The architecture must remain compatible with adding Gemini AI processing through secure Edge Functions in a later phase.

IMPORTANT:
Never expose API keys or service-role credentials in frontend code.

==================================================
2. PHASE 1 OBJECTIVE

Implement only the following functional foundation:

Authentication

Application shell

Database schema

Row Level Security

Private document storage

Case creation

Case listing

Case workspace

Multi-document upload

Document persistence

Document serial numbering

Basic document status tracking

Backend function structure for future processing

Audit-log foundation

Seed database records for backend testing

Hide seeded/demo records from normal application views

Everything must persist in PostgreSQL.

==================================================
3. APPLICATION STRUCTURE

Create these primary routes:

/
/login
/dashboard
/cases
/cases/new
/cases/:caseId
/records
/audit

For this phase:

Dashboard:
Show only basic database-backed counts.

Cases:
Show actual cases belonging to the authenticated officer.

New Case:
Create a real database case.

Case Workspace:
Show case information and allow real document uploads.

Records:
Create the page structure but it can remain minimal because verified records are implemented later.

Audit:
Show basic audit events already generated by Phase 1 actions.

Do not create fake statistics.

==================================================
4. AUTHENTICATION

Implement email/password authentication using Lovable Cloud authentication.

Unauthenticated users must be redirected to:

/login

Authenticated users can access the application.

Create:

profiles

with:

id UUID PRIMARY KEY
full_name TEXT
email TEXT
role TEXT DEFAULT 'OFFICER'
department TEXT
created_at TIMESTAMPTZ
updated_at TIMESTAMPTZ

Automatically create a profile when a new authenticated user registers/logs in for the first time.

Default role:

OFFICER

Keep role architecture extensible for future:

ADMIN
REVIEWER
SUPERVISOR

but do not implement complex role management yet.

==================================================
5. DATABASE SCHEMA

Create the following tables now.

profiles

id UUID PRIMARY KEY
full_name TEXT
email TEXT
role TEXT
department TEXT
created_at TIMESTAMPTZ
updated_at TIMESTAMPTZ

cases

id UUID PRIMARY KEY
case_id TEXT UNIQUE NOT NULL
project_name TEXT NOT NULL
district TEXT
village TEXT
acquisition_reference TEXT
description TEXT
status TEXT NOT NULL DEFAULT 'DRAFT'
created_by UUID NOT NULL
is_demo BOOLEAN NOT NULL DEFAULT false
created_at TIMESTAMPTZ DEFAULT now()
updated_at TIMESTAMPTZ DEFAULT now()

Case status values should support:

DRAFT
PROCESSING
PENDING_REVIEW
VERIFIED
INCONSISTENT

documents

id UUID PRIMARY KEY
case_id UUID NOT NULL
serial_number INTEGER NOT NULL
file_name TEXT NOT NULL
storage_path TEXT NOT NULL
mime_type TEXT NOT NULL
file_size BIGINT
document_type TEXT
processing_status TEXT NOT NULL DEFAULT 'UPLOADED'
processing_stage TEXT
overall_confidence NUMERIC
uploaded_by UUID NOT NULL
is_demo BOOLEAN NOT NULL DEFAULT false
uploaded_at TIMESTAMPTZ DEFAULT now()
processed_at TIMESTAMPTZ

Unique constraint:

(case_id, serial_number)

Processing status must support:

UPLOADED
PROCESSING
COMPLETED
FAILED

Processing stage must support:

INGESTION
DOCUMENT_UNDERSTANDING
CLASSIFICATION
EXTRACTION
VALIDATION
COMPLETED

document_extractions

id UUID PRIMARY KEY
document_id UUID NOT NULL
document_type TEXT
extracted_data JSONB
extraction_confidence NUMERIC
model_name TEXT
created_at TIMESTAMPTZ DEFAULT now()
updated_at TIMESTAMPTZ DEFAULT now()

This table will be populated by Gemini processing in a later phase.

extracted_fields

id UUID PRIMARY KEY
document_id UUID NOT NULL
field_name TEXT
field_value TEXT
confidence NUMERIC
validation_status TEXT
validation_reason TEXT
source_reference TEXT
created_at TIMESTAMPTZ DEFAULT now()

This table will support field-level validation later.

legacy_land_records

id UUID PRIMARY KEY
land_record_id TEXT UNIQUE NOT NULL
owner_name TEXT
survey_number TEXT
khasra_number TEXT
village TEXT
district TEXT
area NUMERIC
area_unit TEXT
status TEXT
is_demo BOOLEAN NOT NULL DEFAULT false
created_at TIMESTAMPTZ DEFAULT now()
updated_at TIMESTAMPTZ DEFAULT now()

This represents the existing institutional/legacy structured record.

It is NOT a second database.

verification_runs

id UUID PRIMARY KEY
case_id UUID NOT NULL
field_validation_status TEXT
cross_document_status TEXT
legacy_comparison_status TEXT
conflict_status TEXT
integrity_score NUMERIC
status TEXT
started_at TIMESTAMPTZ
completed_at TIMESTAMPTZ
verified_by UUID
verified_at TIMESTAMPTZ

verification_field_results

id UUID PRIMARY KEY
verification_run_id UUID NOT NULL
field_name TEXT
source_type TEXT
submitted_value TEXT
legacy_value TEXT
match_status TEXT
confidence NUMERIC
reason TEXT
created_at TIMESTAMPTZ DEFAULT now()

ownership_history

id UUID PRIMARY KEY
case_id UUID NOT NULL
land_record_id TEXT
previous_owner TEXT
new_owner TEXT
mutation_number TEXT
mutation_date DATE
source_document_id UUID
created_at TIMESTAMPTZ DEFAULT now()

verified_land_records

id UUID PRIMARY KEY
land_record_id TEXT UNIQUE NOT NULL
case_id UUID NOT NULL
owner_name TEXT
survey_number TEXT
khasra_number TEXT
village TEXT
district TEXT
area NUMERIC
area_unit TEXT
registration_number TEXT
registration_date DATE
mutation_number TEXT
verification_status TEXT
integrity_score NUMERIC
verified_by UUID
verified_at TIMESTAMPTZ
is_demo BOOLEAN NOT NULL DEFAULT false
created_at TIMESTAMPTZ DEFAULT now()
updated_at TIMESTAMPTZ DEFAULT now()

audit_logs

id UUID PRIMARY KEY
case_id UUID
document_id UUID
user_id UUID NOT NULL
action TEXT NOT NULL
description TEXT
old_value JSONB
new_value JSONB
created_at TIMESTAMPTZ DEFAULT now()

processing_steps

id UUID PRIMARY KEY
document_id UUID NOT NULL
step_name TEXT NOT NULL
status TEXT NOT NULL
message TEXT
started_at TIMESTAMPTZ
completed_at TIMESTAMPTZ
metadata JSONB

==================================================
6. RELATIONSHIPS

Create proper foreign keys.

cases.created_by
→ profiles.id

documents.case_id
→ cases.id

documents.uploaded_by
→ profiles.id

document_extractions.document_id
→ documents.id

extracted_fields.document_id
→ documents.id

verification_runs.case_id
→ cases.id

verification_runs.verified_by
→ profiles.id

verification_field_results.verification_run_id
→ verification_runs.id

ownership_history.case_id
→ cases.id

ownership_history.source_document_id
→ documents.id

verified_land_records.case_id
→ cases.id

verified_land_records.verified_by
→ profiles.id

audit_logs.case_id
→ cases.id

audit_logs.document_id
→ documents.id

audit_logs.user_id
→ profiles.id

processing_steps.document_id
→ documents.id

==================================================
7. ROW LEVEL SECURITY

Enable RLS on all application tables.

An authenticated officer must only be able to access application data they are authorized to access.

For MVP:

authenticated users can access their own profile

authenticated users can create cases

authenticated users can access cases they created

authenticated users can access documents belonging to their accessible cases

authenticated users can create/upload documents into their accessible cases

authenticated users can access processing information for accessible documents

authenticated users can access verification information for accessible cases

authenticated users can access audit logs for accessible cases

Legacy records can be readable by authenticated officers because they represent institutional reference data.

Do not disable RLS merely to make the application work.

Never depend only on frontend route protection.

==================================================
8. PRIVATE STORAGE

Create a private storage bucket:

land-documents

Documents must NOT be publicly accessible.

Storage structure:

cases/{case_id}/{document_id}/{filename}

Use authenticated access.

Do not expose service credentials.

The frontend should only obtain files through authorized access.

Support:

PDF
JPG
JPEG
PNG

Validate file type before upload.

Add a reasonable file-size limit suitable for an MVP.

If upload fails:

preserve the case

show an error

do not create a broken document record

==================================================
9. CASE CREATION

Create a real New Case workflow.

Fields:

Project Name
District
Village
Acquisition Reference
Description

Do NOT ask for:

Survey Number
Khasra Number
Owner

Those will be extracted later from documents.

When the officer creates a case:

Generate a case ID in this format:

LA-2026-00001

The numeric portion must be generated safely and sequentially.

Do not generate random case IDs.

Initial status:

DRAFT

Create an audit log:

CASE_CREATED

After successful creation, redirect to:

/cases/:caseId

==================================================
10. CASE WORKSPACE

Create a functional case workspace.

Display:

Case ID
Project Name
District
Village
Acquisition Reference
Status

Provide:

UPLOAD DOCUMENTS

Allow multiple PDF/image files.

For every uploaded document:

create a document UUID

upload the file to private storage

assign the next serial number

create the documents row

create an audit log

Serial numbers:

01
02
03
04
05

The serial number must be based on upload order within that case.

Do not rely on the filename.

Display a basic document table:

S.No.
Filename
Type
Upload Status
Processing Status
Actions

Document type can initially show:

Pending Classification

because AI classification is a later phase.

==================================================
11. DOCUMENT STATUS FOUNDATION

Every document must have real backend status.

Initial state:

processing_status = UPLOADED

processing_stage = null

Later Edge Functions will update:

PROCESSING
COMPLETED
FAILED

and:

INGESTION
DOCUMENT_UNDERSTANDING
CLASSIFICATION
EXTRACTION
VALIDATION
COMPLETED

Do not fake processing in this phase.

The UI may show:

Uploaded
Awaiting Processing

Do NOT display fake "AI completed" messages.

==================================================
12. AUDIT LOGGING

Implement a reusable backend audit logging mechanism.

At minimum create audit entries for:

CASE_CREATED
DOCUMENT_UPLOADED

Prepare the architecture for:

DOCUMENT_PROCESSING_STARTED
DOCUMENT_CLASSIFIED
FIELDS_EXTRACTED
FIELD_VALIDATION_COMPLETED
CROSS_DOCUMENT_CHECK_COMPLETED
LEGACY_COMPARISON_COMPLETED
CONFLICT_DETECTED
DOCUMENT_VIEWED
FIELD_EDITED
VERIFICATION_STARTED
RECORD_VERIFIED
RECORD_MARKED_FOR_REVIEW
RECORD_MARKED_INCONSISTENT

Do not implement all future actions yet.

Make audit logging reusable so later Edge Functions can call the same mechanism.

==================================================
13. BACKEND FUNCTION STRUCTURE

Create the server-side function structure required for later phases.

Create placeholders/stubs for:

create-case
process-document
run-validation
run-reconciliation
compare-legacy-record
calculate-integrity-score
finalize-verification

IMPORTANT:

Do not implement fake AI processing inside these functions.

For this phase, only establish the secure backend architecture and authentication checks.

The actual Gemini processing will be implemented in Phase 2/3.

Every server-side function must verify the authenticated user before operating on application data.

==================================================
14. GEMINI API

Do NOT implement Gemini processing yet.

However, structure the backend so Gemini can later be called only from an Edge Function.

Do NOT put:

GEMINI_API_KEY

in React/frontend code.

Do NOT hard-code API keys anywhere.

Do NOT create a client-side Gemini request.

If Lovable Cloud supports backend secrets, leave the architecture ready for:

GEMINI_API_KEY

to be added securely later.

==================================================
15. SEED DATA

Seed the database with synthetic data for backend testing.

IMPORTANT:

The seeded data must NOT appear in normal Preview application screens.

Every seed record must use:

is_demo = true

Seed one synthetic case:

Case ID:
LA-2026-00027

Project:
Bhopal–Sehore Road Expansion

District:
Sehore

Village:
Barkheda

Acquisition Reference:
DEMO-2026-001

Status:
DRAFT

Seed one legacy record:

Land Record ID:
LR-000124

Owner:
Ramesh Sharma

Survey:
124/2

Khasra:
87/3

Village:
Barkheda

District:
Sehore

Area:
2.35

Unit:
hectare

Mark this legacy record:

is_demo = true

Do NOT seed fake uploaded document files into storage unless actual valid synthetic files are available.

Do not create fake document rows pointing to nonexistent storage objects.

The purpose of seed data is to verify database relationships and future backend processing.

==================================================
16. DEMO DATA VISIBILITY

This is extremely important.

Normal application screens must behave as if the system is empty for a newly logged-in officer.

Therefore:

Default queries for:

Dashboard
Cases
Documents
Records
Audit

must exclude:

is_demo = true

Demo records must not appear in normal lists.

Do not show:

"Demo Case"
"Seeded Data"
"Sample Data"
"Demo Mode"

anywhere in the normal application.

Do not place demo banners on the dashboard.

The database may contain demo rows, but the normal user experience must remain clean.

Later we may add an explicit demo/testing switch.

==================================================
17. DASHBOARD

Create a minimal dashboard.

Do not spend credits on advanced dashboard design.

Display only real database-backed values:

Total Cases
Documents Uploaded
Documents Awaiting Processing

These counts must exclude:

is_demo = true

If there are no real user cases, show:

0

Do not fabricate values.

Include:

New Case

button.

==================================================
18. CASE LIST

Create:

/cases

Show a simple table:

Case ID
Project
District
Village
Status
Documents
Updated
Action

Only show non-demo cases.

Clicking a case opens:

/cases/:caseId

==================================================
19. RECORDS FOUNDATION

Create:

/records

For Phase 1 it may display:

"No verified land records yet."

unless actual non-demo verified records exist.

Do not create fake verified records.

Prepare the page so Phase 4/5 can populate it from:

verified_land_records

==================================================
20. AUDIT FOUNDATION

Create:

/audit

Show actual audit records belonging to the authenticated officer's accessible cases.

Display:

Action
Description
Case
Document
Timestamp

Only display non-demo case activity in the normal interface.

If no real activity exists:

"No audit activity yet."

==================================================
21. BASIC APPLICATION SHELL

Create a simple professional application shell.

Persistent left sidebar:

LAND RECORD
INTELLIGENCE SYSTEM

Navigation:

Home
Cases
Records
Audit Trail

Top bar:

Current page title
Authenticated officer
Logout

Keep the design clean and functional.

Do NOT spend significant implementation effort on visual polish.

The important requirement is that navigation and data actually work.

==================================================
22. ERROR HANDLING

Implement proper error handling.

Cases:

Database failure
Authentication failure
Unauthorized access
Invalid file
File too large
Storage upload failure
Case creation failure
Document creation failure

Never silently fail.

Show concise user-facing errors.

Never expose:

database credentials
service-role credentials
API keys
internal secrets

==================================================
23. DATA INTEGRITY

Important:

Do not allow orphan documents.

Do not allow duplicate serial numbers inside a case.

Do not create a document database row before successful storage upload.

Do not allow unauthenticated case creation.

Do not allow users to access arbitrary case IDs by changing the URL.

Do not allow users to access arbitrary storage objects.

Use database constraints and RLS wherever appropriate.

==================================================
24. TEST CHECKLIST

Before considering Phase 1 complete, verify these real workflows:

TEST 1:
Open application while logged out.

Expected:
Login page.

TEST 2:
Authenticate.

Expected:
Dashboard opens.

TEST 3:
Create a new case.

Expected:
Case row is actually inserted into PostgreSQL.

TEST 4:
Refresh the browser.

Expected:
Case still exists.

TEST 5:
Open the case.

Expected:
Case workspace loads from database.

TEST 6:
Upload one PDF.

Expected:
File exists in private storage.

Expected:
Document row exists in documents table.

Expected:
Serial number = 01.

Expected:
Audit log contains DOCUMENT_UPLOADED.

TEST 7:
Upload four more documents.

Expected:
Serial numbers:

01
02
03
04
05

TEST 8:
Refresh the page.

Expected:
All documents remain visible.

TEST 9:
Check dashboard.

Expected:
Counts reflect actual database values.

TEST 10:
Open another user's data is not possible through URL manipulation or frontend tricks.

Expected:
RLS prevents unauthorized access.

TEST 11:
Check demo seed data.

Expected:
Seed data exists in database.

Expected:
Seed data does NOT appear in normal dashboard/case/document/record screens.

==================================================
25. IMPORTANT IMPLEMENTATION PRINCIPLE

This phase is NOT about making the application look finished.

This phase is about creating the real foundation.

The following must be REAL:

Authentication
Database
RLS
Storage
Case creation
Document upload
Document persistence
Serial numbering
Audit logging
Backend function structure

Do not simulate these with local arrays, static JSON, fake delays, or hardcoded frontend objects.

Use the database as the source of truth.

==================================================
26. STOP CONDITION

After implementing Phase 1:

DO NOT automatically continue implementing the AI/OCR pipeline.

Stop after the foundation is functional.

The next phase will separately implement:

Gemini document understanding
OCR/extraction
document classification
structured JSON extraction
processing_steps
real-time processing
field-level confidence
validation

We will test Phase 1 before adding those features.

FINAL REQUIREMENT:

Build the foundation completely and functionally, but keep the implementation intentionally narrow so that later phases can be added without rewriting the architecture.

This project was built with [Lovable](https://lovable.dev).

## Build with Lovable

Continue developing this project in the [Lovable editor](https://lovable.dev/projects/fde8cbdb-9a2f-46f1-ab3f-4645f0cbc481).

- **Ship faster**: describe what you want to build and Lovable handles the code.
- **Stay in sync**: every change made in Lovable is committed straight to this repository.
- **Full ownership**: this code is yours. Push to `main` on GitHub and your changes sync back into Lovable, ready for your next prompt.

## Development

Prefer working locally? You need Node.js and npm — [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```
